/**
 * Root entry for expo-router: route group `(app)` is invisible in the URL,
 * but a root `app/index.tsx` keeps the route table and tooling predictable.
 */
export { default } from "./(app)/index";
